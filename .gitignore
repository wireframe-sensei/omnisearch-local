# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (none in use yet, but keep secrets out if that changes)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vscode/

# Rust build artifacts (already covered by src-tauri/.gitignore; restated here since
# this directory reaches multiple GB and is worth guarding against at the root too)
src-tauri/target/